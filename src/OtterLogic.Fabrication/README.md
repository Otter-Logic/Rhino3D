# Fabrication

A future domain, sibling to StructuralForm — not a folder inside Core.

Unrolling, planarisation, nesting, joint generation, toolpaths, DXF export.

Rules when it arrives: its own types and its own logic together, depends on Core
and on nothing else in `src/`, and no UI. If it turns out to need something from
StructuralForm, that something belongs in Core.
